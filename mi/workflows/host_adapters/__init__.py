"""Host adapters (derived artifacts + best-effort registration).

Adapters are intentionally decoupled from MI core storage; they only operate on
host workspaces via derived, regeneratable artifacts under `.mi/generated/<host>/...`.
"""

